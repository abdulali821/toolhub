# Morse Code Encoder / Decoder

Tool id: `morse-codec`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/morse-codec.test.ts`
