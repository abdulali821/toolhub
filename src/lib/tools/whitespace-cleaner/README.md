# Whitespace Cleaner

Tool id: `whitespace-cleaner`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/whitespace-cleaner.test.ts`
