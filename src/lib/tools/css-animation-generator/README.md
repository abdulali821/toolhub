# CSS Animation Generator

Tool id: `css-animation-generator`

Build CSS `@keyframes` animations with live preview. Controls cover duration, delay, iteration, direction, fill-mode, easing, and keyframe steps for `transform`, `opacity`, and `background-color`.
