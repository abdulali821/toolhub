# Query String JSON

Tool id: `query-string-json`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/query-string-json.test.ts`
