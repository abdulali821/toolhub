# Color Blindness Simulator

Tool id: `color-blindness-simulator`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/color-blindness-simulator.test.ts`
