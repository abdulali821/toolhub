# Remove Empty Lines

Tool id: `remove-empty-lines`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/remove-empty-lines.test.ts`
