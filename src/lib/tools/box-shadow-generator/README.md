# Box Shadow Generator

Tool id: `box-shadow-generator`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/box-shadow-generator.test.ts`
