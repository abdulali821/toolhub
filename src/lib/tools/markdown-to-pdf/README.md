# Markdown to PDF

Tool id: `markdown-to-pdf`

Convert Markdown notes into a downloadable PDF in the browser via pdf-lib.

## Develop

1. Adjust block parsing / layout in `index.ts`
2. Refine UI in `ui.svelte`
3. Expand `tests/tools/markdown-to-pdf.test.ts`
