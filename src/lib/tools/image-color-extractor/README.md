# Image Color Extractor

Tool id: `image-color-extractor`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/image-color-extractor.test.ts`
