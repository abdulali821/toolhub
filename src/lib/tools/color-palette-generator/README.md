# Color Palette Generator

Tool id: `color-palette-generator`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/color-palette-generator.test.ts`
