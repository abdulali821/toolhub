# Reading Time Estimator

Tool id: `reading-time-estimator`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/reading-time-estimator.test.ts`
