# Add Prefix/Suffix

Tool id: `add-prefix-suffix`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/add-prefix-suffix.test.ts`
