# Random String Generator

Tool id: `random-string-generator`

## Develop

1. Implement real logic in `run` inside `index.ts`
2. Adjust UI in `ui.svelte`
3. Add tests under `tests/tools/random-string-generator.test.ts`
